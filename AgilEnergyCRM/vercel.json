{
  "buildCommand": "npm run build -- --configuration=production",
  "outputDirectory": "dist/stage-pfe",
  "framework": "angular",
  "routes": [
    { "handle": "filesystem" },
    { "src": "/(.*)", "dest": "/index.html" }
  ]
}