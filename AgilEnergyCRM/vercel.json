{
  "buildCommand": "npm run build -- --configuration=production",
  "outputDirectory": "dist/stage-pfe/browser",
  "framework": "angular",
  "routes": [
    {
      "src": "/[^.]+",
      "dest": "/index.html"
    }
  ]
}